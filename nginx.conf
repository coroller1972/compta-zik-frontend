index index.html;

location = /internal/authelia/authz {
      internal;

      proxy_pass http://authelia-svc:9091/api/verifiy;
      proxy_pass_request_body off;
      proxy_set_header Content-Length "";

      proxy_set_header X-Original-URL https://$http_host$request_uri;
      proxy_set_header X-Forwarded-Method $request_method;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-Host $http_host;
      proxy_set_header X-Forwarded-Uri $request_uri;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Real-IP $remote_addr;
  }

  location = /index.html {
      auth_request /internal/authelia/authz;
      error_page 401 =302 https://authelia-ui-core.apps.dev-app-hub.0k6e.p1.openshiftapp.com/?rd=https://$http_host$request_uri;
      error_page 403 = /403.html;

      add_header Cache-Control "no-store";
      try_files $uri =404;
  }

  location /src/ {
      auth_request /internal/authelia/authz;
      error_page 401 =302 https://authelia-ui-core.apps.dev-app-hub.0k6e.p1.openshiftapp.com/?rd=https://$http_host$request_uri;
      error_page 403 = /403.html;

      add_header Cache-Control "public, max-age=300";
      try_files $uri =404;
  }

  location /api/ {
      auth_request /internal/authelia/authz;
      error_page 401 =302 https://authelia-ui-core.apps.dev-app-hub.0k6e.p1.openshiftapp.com/?rd=https://$http_host$request_uri;
      error_page 403 = /403.html;

      proxy_pass http://mserv-compta-zik-svc:8081/api/;
      proxy_http_version 1.1;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
  }

  location / {
      auth_request /internal/authelia/authz;
      error_page 401 =302 https://authelia-ui-core.apps.dev-app-hub.0k6e.p1.openshiftapp.com/?rd=https://$http_host$request_uri;
      error_page 403 = /403.html;

      add_header Cache-Control "no-store";
      try_files $uri $uri/ /index.html;
  }
